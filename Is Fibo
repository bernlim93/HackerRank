T = int(input())

for i in range(T):
    N = int(input())
    X = 0
    Y = 1
    while(X < N):
        X += Y
        Y = X-Y

    if X == N:
        print("IsFibo")
    else:
        print("IsNotFibo")
        
    
